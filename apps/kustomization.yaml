apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Application deployments
# Use environment-specific overlays for different configurations

resources:
  - base

# Default namespace for applications
namespace: apps