apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: whoami
      app.kubernetes.io/component: demo-app
      app.kubernetes.io/part-of: k3s-apps

namespace: apps